name: EPH Auth Latency Probe

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  auth-latency-probe:
    name: EPH Canli Giris Gecikme Olcumu
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Tamamlandi
        run: echo "Auth latency probe tamamlandı ve devre dışı bırakıldı."
